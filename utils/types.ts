export type MenuType = {
  id: number;
  name: string;
  href: string;
};

export type ServicesType = {
  id: number;
  skill: string;
  description: string;
  icon: React.ReactNode;
};

export type TestimonialsType = {
  id: number;
  img: string;
  imgAlt: string;
  description: string;
  name: string;
  title: string;
};
